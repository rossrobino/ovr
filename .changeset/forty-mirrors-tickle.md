---
"ovr": major
---

remove!: Remove `Context.head`, `Context.layouts`, and `Context.base`

BREAKING CHANGES: ovr now expects the full page to be passed into each handler. TODO - return response from next to set layout?
